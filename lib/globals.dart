// define a few global colors
// import colors
import 'package:flutter/material.dart';

class Colors {
  static const Color green = Color.fromRGBO(3, 250, 20, 1);
  static const Color red = Color.fromRGBO(237, 27, 35, 1);

  // Add more colors as needed
}
